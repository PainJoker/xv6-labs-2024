Q1: Looking at the backtrace output, which function called syscall?
A: usertrap() at kernel/trap.c:67

Q2: What is the value of p->trapframe->a7 and what does that value represent?
A: p->trapframe->a7 = 7, which represents label of SYS_exec syscall.

Q3: What was the previous mode that the CPU was in?
A: $sstatus = 0x200000022, SPP bit = 0 indicates that the previous mode was user mode.

Q4: Write down the assembly instruction the kernel is panicing at. Which register corresponds to the variable num?
A: lw a3, 0(zero), corresponding register is a3.

Q5: Why does the kernel crash?
A: Address 0 is not mapped in the kernel addresss space, and scause value confirms.

Q6: What is the name of the process taht was running when the kernel paniced? What is its pid?
A: initcode, pid=1

